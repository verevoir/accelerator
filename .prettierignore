# Intentionally-malformed test fixtures: these hold non-JSON / non-parseable
# content on purpose, to exercise the manifest resolver's graceful-degrade
# paths. Prettier cannot parse them, so it is scoped out here narrowly — valid
# fixtures stay formatted and checked.
tests/fixtures/non-json-dir/aigency.json
tests/fixtures/empty-dir/.gitkeep

# Committed verbatim (the design/build spec as briefed). Prettier would reflow
# its indented tool-class list and heading spacing, so it is scoped out to keep
# the record byte-faithful to the brief.
docs/2026-07-24-pi-plugin-and-permissions.md
